@model IEnumerable<_403Project1.Models.Parent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PLastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PLogin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PLastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PLogin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParentID })
        </td>
    </tr>
}

</table>
